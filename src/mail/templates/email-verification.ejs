<p>Hey <%= name %></p>

<p>
  This is your otp: <%= otp %>
</p>

<p>
  Regards,
  <br />
  <%= process.env.APP_NAME %>
</p>



<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your One-Time Password - <%= process.env.APP_NAME %></title>
    <style>
      body {
        margin: 0;
        padding: 0;
        background-color: #eef2f7;
        font-family: "Inter", Arial, sans-serif;
        color: #2e2e2e;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        background-color: #ffffff;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
        border: 1px solid #e3e6ec;
      }

      .header {
        background-color: #ffffff;
        border-bottom: 3px solid #004aad;
        text-align: center;
        padding: 25px 20px;
      }

      .header h1 {
        margin: 0;
        font-size: 22px;
        font-weight: 700;
        color: #004aad;
      }

      .content {
        padding: 35px 30px;
        text-align: left;
        line-height: 1.7;
      }

      .content p {
        font-size: 16px;
        margin-bottom: 20px;
      }

      .otp-section {
        background-color: #f7f9fc;
        border: 1px dashed #b6c4e3;
        border-radius: 10px;
        text-align: center;
        padding: 25px 20px;
        margin: 25px 0;
      }

      .otp-code {
        display: inline-block;
        color: #004aad;
        font-size: 30px;
        font-weight: 700;
        letter-spacing: 6px;
      }

      .note {
        font-size: 14px;
        color: #666;
        text-align: center;
        margin-top: 20px;
      }

      .divider {
        border-top: 1px solid #e8eaee;
        margin: 35px 0;
      }

      .footer {
        background: #f8f9fb;
        text-align: center;
        padding: 20px;
        font-size: 13px;
        color: #777;
      }

      @media only screen and (max-width: 600px) {
        .container {
          width: 92%;
          margin: 20px auto;
        }
        .content {
          padding: 25px 20px;
        }
        .otp-code {
          font-size: 26px;
          letter-spacing: 4px;
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <!-- Header -->
      <div class="header">
        <h1><%= process.env.APP_NAME %> Verification Code</h1>
      </div>

      <!-- Body -->
      <div class="content">
        <p>Hi <strong><%= name %></strong>,</p>
        <p>
          To complete your verification for
          <strong><%= process.env.APP_NAME %></strong>, please use the
          One-Time Password (OTP) below:
        </p>

        <!-- OTP Section -->
        <div class="otp-section">
          <div class="otp-code"><%= otp %></div>
        </div>

        <p class="note">
          This code will expire in <strong>5 minutes</strong> for security
          reasons.<br />
          If you did not request this, you can safely ignore this email.
        </p>

        <div class="divider"></div>

        <p style="font-size: 14px; color: #555; text-align: center;">
          Need help? Contact our support team anytime at
          <a
            href="mailto:<%= process.env.MAIL_FROM_ADDRESS.toLowerCase() %>"
            style="color: #004aad; text-decoration: none; font-weight: 500;"
            >@<%= process.env.MAIL_FROM_ADDRESS.toLowerCase() %></a
          >.
        </p>
      </div>

      <!-- Footer -->
      <div class="footer">
        &copy; <%= new Date().getFullYear() %> <%= process.env.APP_NAME %>.
        All rights reserved.<br />
        This is an automated message, please do not reply.
      </div>
    </div>
  </body>
</html>